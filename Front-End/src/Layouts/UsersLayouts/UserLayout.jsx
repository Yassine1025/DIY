import React from 'react';
import { Outlet } from 'react-router-dom';

const UserLayout = () => {
    return (
        <div>
            login succes 
        </div>
    );
}

export default UserLayout;
